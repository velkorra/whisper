# Faster-Whisper Audio Transcriber

Мощный инструмент для транскрибации аудиофайлов в текст с использованием библиотеки faster-whisper. Поддерживает обработку больших файлов, оптимизацию памяти и различные модели Whisper.

## Особенности

- 🚀 **Высокая производительность** с библиотекой faster-whisper
- 🧠 **Поддержка различных моделей** (от tiny до large-v3)
- 💻 **GPU и CPU поддержка** с автоматической оптимизацией
- 🔇 **VAD фильтр** для удаления тишины из больших файлов
- 🔄 **Защита от зацикливания** при обработке повторяющихся сегментов
- 📊 **Отображение прогресса** в реальном времени
- 💾 **Автоматическое резервное копирование** выходных файлов
- 🌐 **Автоопределение языка** или ручная установка

## Установка

### Автоматическая установка

Используйте прилагаемый файл `requirements.txt` для установки всех зависимостей:

```bash
pip install -r requirements.txt
```

### Ручная установка

```bash
pip install faster-whisper==1.1.1
```

Для GPU поддержки (CUDA 12.8):
```bash
pip install torch==2.7.0+cu128 torchaudio==2.7.0+cu128 --index-url https://download.pytorch.org/whl/cu128
```

### Системные требования

- **Python**: 3.9 или выше
- **GPU**: NVIDIA с поддержкой CUDA 12+ и cuDNN 9
- **RAM**: 8+ GB для больших файлов
- **VRAM**: 2-10 GB в зависимости от модели

### GPU требования

Для работы на GPU необходимы:
- **cuBLAS** для CUDA 12
- **cuDNN 9** для CUDA 12

> **Примечание**: Последние версии ctranslate2 поддерживают только CUDA 12 и cuDNN 9. Для более старых версий CUDA используйте ctranslate2==3.24.0 (CUDA 11) или ctranslate2==4.4.0 (CUDA 12 + cuDNN 8).

## Использование

### Базовое использование

```bash
python transcriber.py audio_file.wav
```

### Расширенные опции

```bash
python transcriber.py audio_file.wav \
    --model_size large-v3 \
    --device cuda \
    --compute_type float16 \
    --language ru \
    --vad_filter \
    --output_file output.txt
```

## Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `audio_file` | Путь к аудиофайлу | **Обязательный** |
| `--model_size` | Размер модели Whisper | `medium` |
| `--language` | Код языка (ru, en, и др.) | Автоопределение |
| `--device` | Устройство (cuda/cpu) | `cuda` |
| `--compute_type` | Тип вычислений | `float16` |
| `--beam_size` | Размер пучка для декодирования | `5` |
| `--vad_filter` | Включить VAD фильтр | `False` |
| `--output_file` | Путь для сохранения текста | `{filename}_transcribed_fw.txt` |
| `--show_full_text` | Показать полный текст в консоли | `False` |

### Доступные модели

- `tiny`, `tiny.en` - базовое качество, очень быстро
- `base`, `base.en` - хорошее качество, быстро  
- `small`, `small.en` - хорошее качество, среднее время
- `medium`, `medium.en` - отличное качество, среднее время
- `large-v1`, `large-v2`, `large-v3` - превосходное качество
- `turbo` - новая быстрая модель с хорошим качеством
- `distil-small.en`, `distil-medium.en` - оптимизированные модели
- `distil-large-v2`, `distil-large-v3` - большие оптимизированные модели

## Доступные модели

### Размеры моделей Whisper

| Модель | Размер | VRAM | Скорость | Качество |
|--------|--------|------|----------|----------|
| `tiny` | ~39 MB | ~1 GB | Очень быстро | Базовое |
| `base` | ~74 MB | ~1 GB | Быстро | Хорошее |
| `small` | ~244 MB | ~2 GB | Среднее | Хорошее |
| `medium` | ~769 MB | ~5 GB | Среднее | Отличное |
| `large-v3` | ~1550 MB | ~10 GB | Медленно | Превосходное |
| `turbo` | ~809 MB | ~6 GB | Быстрое | Отличное |

### Специализированные модели

- `*.en` - только английский язык (быстрее)
- `distil-*` - дистиллированные модели (меньше, быстрее)
- `distil-large-v3` - оптимизированная большая модель

## Оптимизация для различных сценариев

### Большие файлы (>100 MB)

```bash
python transcriber.py large_audio.wav \
    --model_size medium \
    --compute_type int8 \
    --beam_size 1 \
    --vad_filter
```

### Максимальное качество

```bash
python transcriber.py audio.wav \
    --model_size large-v3 \
    --compute_type float16 \
    --beam_size 5 \
    --language ru
```

### Быстрая обработка

```bash
python transcriber.py audio.wav \
    --model_size tiny \
    --compute_type int8 \
    --beam_size 1 \
    --vad_filter
```

### Ограниченные ресурсы (CPU)

```bash
python transcriber.py audio.wav \
    --device cpu \
    --model_size base \
    --compute_type int8 \
    --vad_filter
```

### Быстрая новая модель (турбо)

```bash
python transcriber.py audio.wav \
    --model_size turbo \
    --compute_type float16 \
    --beam_size 5
```

### Оптимизированная дистиллированная модель

```bash
python transcriber.py audio.wav \
    --model_size distil-large-v3 \
    --compute_type float16 \
    --language en
```

## Коды языков

Поддерживаемые языки (основные):

- `ru` - Русский
- `en` - Английский
- `es` - Испанский
- `fr` - Французский
- `de` - Немецкий
- `it` - Итальянский
- `ja` - Японский
- `ko` - Корейский
- `zh` - Китайский

[Полный список языков](https://github.com/openai/whisper#available-models-and-languages)

## Поддерживаемые форматы

Поддерживаются все популярные аудиоформаты:
- WAV, MP3, FLAC, OGG
- M4A, AAC, WMA
- MP4, AVI (аудиодорожка)

## Решение проблем

### Ошибки памяти

```bash
# Используйте меньшую модель
--model_size base

# Или int8 вычисления
--compute_type int8

# Включите VAD фильтр
--vad_filter
```

### Медленная обработка

```bash
# Уменьшите beam_size
--beam_size 1

# Включите VAD для удаления тишины
--vad_filter

# Используйте GPU
--device cuda
```

### Пустой результат

- Проверьте, что аудио содержит речь
- Отключите VAD фильтр: уберите `--vad_filter`
- Попробуйте другой формат аудиофайла
- Проверьте громкость аудио

## Примеры использования

### Транскрибация интервью

```bash
python transcriber.py interview.mp3 \
    --model_size large-v3 \
    --language ru \
    --vad_filter \
    --show_full_text
```

### Обработка лекции

```bash
python transcriber.py lecture.wav \
    --model_size medium \
    --compute_type int8 \
    --vad_filter \
    --output_file lecture_notes.txt
```

### Быстрая обработка множества файлов

```bash
for file in *.mp3; do
    python transcriber.py "$file" \
        --model_size base \
        --beam_size 1 \
        --vad_filter
done
```

## Техническая информация

### Производительность (по данным faster-whisper)

**Large-v2 модель на GPU (RTX 3070 Ti 8GB):**
- faster-whisper (float16): 1м03с, 4525MB VRAM
- faster-whisper (batch_size=8): 17с, 6090MB VRAM  
- faster-whisper (int8): 59с, 2926MB VRAM
- Для сравнения: openai/whisper (float16): 2м23с, 4708MB VRAM

**Small модель на CPU (Intel i7-12700K, 8 потоков):**
- faster-whisper (float32): 2м37с, 2257MB RAM
- faster-whisper (batch_size=8): 1м06с, 4230MB RAM
- faster-whisper (int8): 1м42с, 1477MB RAM

### Особенности реализации

- Защита от зацикливания при повторяющихся сегментах
- Периодическая очистка памяти для больших файлов
- Автоматическое резервное копирование результатов
- Отображение прогресса и скорости обработки

## Лицензия

Этот проект использует библиотеку faster-whisper (MIT License), которая основана на модели Whisper от OpenAI.

## Связанные проекты

- [faster-whisper](https://github.com/SYSTRAN/faster-whisper) - оригинальная библиотека
- [OpenAI Whisper](https://github.com/openai/whisper) - оригинальная модель
- [CTranslate2](https://github.com/OpenNMT/CTranslate2) - движок для ускорения

## Поддержка

При возникновении проблем:

1. Проверьте системные требования
2. Попробуйте меньшую модель или int8 вычисления
3. Включите VAD фильтр для больших файлов
4. Убедитесь в корректности аудиоформата